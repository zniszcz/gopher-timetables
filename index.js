import moment from 'moment';

class Gopher {
    constructor({name, avalaibleFrom, avalaibleTo}) {
        this.name = name;
        this.avalaibleFrom = avalaibleFrom;
        this.avalaibleTo = avalaibleTo;
    }
}